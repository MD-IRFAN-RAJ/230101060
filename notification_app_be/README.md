# Backend

Express server with mock data.

## Setup

```
npm install
npm start
```

Server runs on `http://localhost:4000`

## API

- `GET /api/notifications` - Get all notifications
- `POST /api/notifications` - Create notification (requires `title`, `message` in body)
